rules_version = '2';

// App uses localStorage for session storage — Firestore not used by the app.
// All access denied to prevent unauthorized use.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
